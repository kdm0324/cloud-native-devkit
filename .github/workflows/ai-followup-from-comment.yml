name: AI PR Follow-up from Comment (Gemini)

on:
  issue_comment:
    types: [created]
  workflow_dispatch:
    inputs:
      pr_number:
        description: "대상 PR 번호"
        required: true
        type: string
      instruction:
        description: "에이전트에게 시킬 작업 지시(/ai 없이 본문만 입력)"
        required: true
        type: string
      dry_run:
        description: "true면 커밋/푸시 없이 수정+검증까지만"
        required: false
        default: "false"
        type: choice
        options: ["false", "true"]

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: ai-pr-followup-${{ github.event.issue.number || inputs.pr_number }}
  cancel-in-progress: true

jobs:
  followup:
    if: >
      (
        github.event_name == 'issue_comment' &&
        github.event.issue.pull_request &&
        startsWith(github.event.comment.body, '/ai') &&
        github.actor == 'kdm0324'
      )
      ||
      (github.event_name == 'workflow_dispatch')

    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo (needed for git context)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Resolve PR number & instruction
        id: ctx
        shell: bash
        run: |
          set -euo pipefail

          if [ "${{ github.event_name }}" = "issue_comment" ]; then
            PR_NUMBER="${{ github.event.issue.number }}"
            RAW="${{ github.event.comment.body }}"
            INSTRUCTION="${RAW#/ai}"
            INSTRUCTION="${INSTRUCTION#"${INSTRUCTION%%[![:space:]]*}"}"
            DRY_RUN="false"
          else
            PR_NUMBER="${{ inputs.pr_number }}"
            INSTRUCTION="${{ inputs.instruction }}"
            DRY_RUN="${{ inputs.dry_run }}"
          fi

          echo "pr_number=$PR_NUMBER" >> "$GITHUB_OUTPUT"
          {
            echo "instruction<<EOF"
            echo "$INSTRUCTION"
            echo "EOF"
          } >> "$GITHUB_OUTPUT"
          echo "dry_run=$DRY_RUN" >> "$GITHUB_OUTPUT"

      - name: Fetch PR metadata
        id: pr
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_REPO: ${{ github.repository }}
        shell: bash
        run: |
          set -euo pipefail
          PR_NUMBER="${{ steps.ctx.outputs.pr_number }}"

          # ⚠️ headRepository.nameWithOwner 대신 owner.login을 사용 (빈 값 방지)
          gh pr view "$PR_NUMBER" \
            --json number,url,state,headRefName,isCrossRepository,headRepositoryOwner \
            > pr.json

          echo "PR_NUMBER=$(jq -r '.number' pr.json)" >> "$GITHUB_OUTPUT"
          echo "PR_URL=$(jq -r '.url' pr.json)" >> "$GITHUB_OUTPUT"
          echo "STATE=$(jq -r '.state' pr.json)" >> "$GITHUB_OUTPUT"
          echo "HEAD_REF=$(jq -r '.headRefName' pr.json)" >> "$GITHUB_OUTPUT"
          echo "IS_CROSS_REPO=$(jq -r '.isCrossRepository' pr.json)" >> "$GITHUB_OUTPUT"
          echo "HEAD_OWNER=$(jq -r '.headRepositoryOwner.login // empty' pr.json)" >> "$GITHUB_OUTPUT"

      - name: Safety gate (open PR + not fork + same owner + ai/* branch only)
        id: gate
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_REPO: ${{ github.repository }}
        shell: bash
        run: |
          set -euo pipefail

          if [ "${{ steps.pr.outputs.STATE }}" != "OPEN" ]; then
            echo "reason=Blocked: PR is not OPEN" >> "$GITHUB_OUTPUT"
            exit 1
          fi

          if [ "${{ steps.pr.outputs.IS_CROSS_REPO }}" = "true" ]; then
            echo "reason=Blocked: PR from fork (isCrossRepository=true)" >> "$GITHUB_OUTPUT"
            exit 1
          fi

          if [ -z "${{ steps.pr.outputs.HEAD_OWNER }}" ]; then
            echo "reason=Blocked: cannot resolve head repo owner" >> "$GITHUB_OUTPUT"
            exit 1
          fi

          if [ "${{ steps.pr.outputs.HEAD_OWNER }}" != "${{ github.repository_owner }}" ]; then
            echo "reason=Blocked: head owner mismatch (${{ steps.pr.outputs.HEAD_OWNER }} != ${{ github.repository_owner }})" >> "$GITHUB_OUTPUT"
            exit 1
          fi

          case "${{ steps.pr.outputs.HEAD_REF }}" in
            ai/*) echo "OK: ai branch" ;;
            *) echo "reason=Blocked: not an ai/* branch" >> "$GITHUB_OUTPUT"; exit 1 ;;
          esac

      - name: Comment and stop if safety gate failed
        if: failure() && steps.gate.outcome == 'failure'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_REPO: ${{ github.repository }}
        shell: bash
        run: |
          set -euo pipefail
          PR_NUMBER="${{ steps.pr.outputs.PR_NUMBER }}"

          cat > pr_comment.txt <<EOF
          ⛔ AI follow-up 실행이 차단되었습니다.
          
          - 사유: ${{ steps.gate.outputs.reason }}
          
          (보안상 fork PR 또는 ai/* 브랜치가 아닌 경우 실행하지 않습니다.)
          EOF
          gh pr comment "$PR_NUMBER" --body-file pr_comment.txt
          exit 1

      - name: Checkout PR branch
        shell: bash
        run: |
          set -euo pipefail
          HEAD_REF="${{ steps.pr.outputs.HEAD_REF }}"
          git fetch origin "$HEAD_REF":"$HEAD_REF"
          git checkout "$HEAD_REF"

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install deps & build core (baseline)
        shell: bash
        run: |
          set -euo pipefail
          npm ci
          npm --workspace packages/core run build

      - name: Agent (Gemini)
        uses: google-github-actions/run-gemini-cli@v0
        with:
          gemini_api_key: ${{ secrets.GEMINI_API_KEY }}
          args: >-
            You are an autonomous coding agent working on the repository cloud-native-devkit.
            You are continuing work on an existing PR branch already checked out in this runner.

            USER INSTRUCTION (from PR comment or manual dispatch):
            ${{ steps.ctx.outputs.instruction }}

            REQUIRED BEHAVIOR:
            1) Internally convert the instruction into a TOON plan (Task/Outcome/Ops/Notes). Do NOT ask questions.
            2) Apply ONLY what is requested. Keep changes minimal and safe.
            3) Do NOT run any install/build/test commands (npm/yarn/pnpm/make/gradle/mvn/etc).
            4) Do NOT run any shell commands at all.
            5) Do not print secrets or environment variables.
            IMPLEMENT NOW by editing repository files only.

      - name: Verify (runbook)
        shell: bash
        run: |
          set -euo pipefail
          npm ci
          npm --workspace packages/core run build
          npm run dev:cli -- doctor

      - name: On verification failure, comment and stop
        if: failure()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_REPO: ${{ github.repository }}
        shell: bash
        run: |
          set -euo pipefail
          PR_NUMBER="${{ steps.pr.outputs.PR_NUMBER }}"

          cat > pr_comment.txt <<'EOF'
          ❌ 검증 실패로 중단했습니다.
          
          - 실행한 커맨드:
            - npm ci
            - npm --workspace packages/core run build
            - npm run dev:cli -- doctor
          
          Actions 로그를 확인하고, 수정 지시를 다시 `/ai ...` 로 남겨주세요.
          EOF
          gh pr comment "$PR_NUMBER" --body-file pr_comment.txt
          exit 1

      - name: Remove agent artifacts (if any)
        shell: bash
        run: |
          set -euo pipefail
          rm -rf .gemini || true

      - name: Dry run - comment back and stop
        if: steps.ctx.outputs.dry_run == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_REPO: ${{ github.repository }}
        shell: bash
        run: |
          set -euo pipefail
          PR_NUMBER="${{ steps.pr.outputs.PR_NUMBER }}"
          echo "🧪 DRY_RUN=true — 수정/검증까지 완료했고, 커밋/푸시는 하지 않았습니다." > pr_comment.txt
          gh pr comment "$PR_NUMBER" --body-file pr_comment.txt
          exit 0

      - name: Commit & push (if changes exist)
        if: steps.ctx.outputs.dry_run != 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        shell: bash
        run: |
          set -euo pipefail
          PR_NUMBER="${{ steps.pr.outputs.PR_NUMBER }}"

          rm -rf .gemini || true

          if git diff --quiet && git diff --cached --quiet; then
            echo "ℹ️ 변경 사항이 감지되지 않아 커밋/푸시하지 않았습니다." > pr_comment.txt
            GH_REPO="${GITHUB_REPOSITORY}" gh pr comment "$PR_NUMBER" --body-file pr_comment.txt
            exit 0
          fi

          git config user.name "ai-bot"
          git config user.email "ai-bot@users.noreply.github.com"
          git add -A
          git commit -m "ai: follow-up from PR comment"
          git push

      - name: Comment back to PR (success)
        if: steps.ctx.outputs.dry_run != 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_REPO: ${{ github.repository }}
        shell: bash
        run: |
          set -euo pipefail
          PR_NUMBER="${{ steps.pr.outputs.PR_NUMBER }}"

          cat > pr_comment.txt <<EOF
          ✅ /ai 지시사항을 반영해 커밋을 푸시했습니다.
          
          - 브랜치: \`${{ steps.pr.outputs.HEAD_REF }}\`
          - PR: ${{ steps.pr.outputs.PR_URL }}
          
          검증:
          - npm ci
          - npm --workspace packages/core run build
          - npm run dev:cli -- doctor
          EOF
          gh pr comment "$PR_NUMBER" --body-file pr_comment.txt
