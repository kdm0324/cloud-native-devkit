{
  "name": "cloud-native-devkit",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "cnd": "npm --workspace packages/cli run dev",
    "dev:cli": "npm --workspace packages/cli run dev",
    "build": "npm --workspace packages/core run build && npm --workspace packages/cli run build",
    "build:release": "npm --workspace packages/core run build && npm --workspace packages/cli run build:release"
  },
  "devDependencies": {
    "@types/node": "^25.0.3"
  }
}
